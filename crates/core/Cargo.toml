[package]
name = "xproxy-core"
version.workspace = true
edition.workspace = true
description = "Core types and traits for xproxy"
license.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tokio-util = "0.7"
http = "1"
